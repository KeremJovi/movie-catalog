version: '3'

services:
  mongodb:
    image: mongo:latest
    container_name: 'mongodb'
    hostname: mongodb
    restart: always
    environment:
      - MONGODB_DATABASE=MovieHaku
    ports:
      - '27017:27017'
